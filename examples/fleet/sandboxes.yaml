version: 1

# Every org on a non-loopback forge must match this. Choose it so that it CANNOT match the
# organisation holding your real code.
org_allowlist: '^forgelab-sandbox'

sandboxes:
  local:
    forge: forgejo
    base_url: http://localhost:3000       # loopback: exempt from the allowlist
    org: forgelab-sandbox
    token_env: FORGELAB_LOCAL_TOKEN       # the NAME of a variable; `make up` prints the export

  # A GitHub sandbox. Uncomment, point it at an org that holds nothing else, and make sure
  # org_allowlist above matches it. base_url is only needed for Enterprise Server.
  #
  # gh:
  #   forge: github
  #   org: forgelab-sandbox
  #   token_env: FORGELAB_GH_TOKEN          # see the README for the token to create

  # A GitLab sandbox: `org` is a group's full path, and may be nested.
  #
  # gl:
  #   forge: gitlab
  #   org: forgelab-sandbox
  #   token_env: FORGELAB_GL_TOKEN

  # An Azure DevOps sandbox is a project inside an organisation; org_allowlist is matched
  # against "org/project". Topics, visibility and the marker guard do not exist there.
  #
  # ado:
  #   forge: azuredevops
  #   org: forgelab-sandbox
  #   project: fleet
  #   token_env: FORGELAB_ADO_TOKEN
